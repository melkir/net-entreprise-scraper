[package]
name = "net-entreprise-scraper"
version = "0.1.2"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = "0.7.5"
serde = { version = "1.0", features = ["derive"] }
console_error_panic_hook = "0.1.7"
regex = "1.12.3"
